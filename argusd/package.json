{
  "name": "argusd",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Argus daemon: eyes and hands for agents, Omarchy first",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "bun build --compile --minify src/main.ts --outfile dist/argusd",
    "serve": "bun src/main.ts serve"
  },
  "devDependencies": {
    "bun-types": "^1.4.2",
    "devtools-protocol": "^0.0.1698617",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  }
}
